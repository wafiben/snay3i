{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["jest"],
    "baseUrl": ".",                     // Required for path mapping
    "paths": {
      "@src/*": ["src/*"]              // This enables @src alias in tests
    }
  },
  "exclude": ["node_modules", "dist"]
}
